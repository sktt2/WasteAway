import { createTheme } from "@mui/material/styles"

export const appTheme = createTheme({
    palette: {
        text: {
            primary: "#2f4858",
        },
        //   primary: blue,
        //   secondary: pink,
    },
    typography: {
        allVariants: {
            color: "#2f4858",
        },
    },
})
